# VOIS Charts React Application
## Setup:
- Run "npm install" to install all necessary packages

### Available Commands
- "test" for running jest tests.
- "build" for building the project
- "start" to start a dev server.